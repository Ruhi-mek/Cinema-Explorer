// Footer Component - Single Responsibility: Footer UI
// This component is now in index.html as static HTML
// Keeping this file for consistency with folder structure
export class Footer {
  // Footer is rendered directly in index.html
  // No need to generate HTML dynamically
}
